name: keepalive

on:
  schedule:
    - cron: "*/8 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot so Render does not sleep
        run: curl -fsS --retry 3 --retry-delay 15 --max-time 90 "https://tg-translator-c7iq.onrender.com/health"
